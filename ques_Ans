Which of the following best describes supervised learning?

✅ Learning from labeled input-output pairs

2. Which of the following are examples of supervised learning tasks?

✅ Email spam classification
❌ Customer segmentation → unsupervised
✅ House price prediction
❌ Image clustering → unsupervised

3. A dataset has 100 samples. Model predicts 85 correctly. Accuracy?

Accuracy = 85 / 100 = 0.85
✅ 0.85

4. Which algorithms are commonly used for supervised learning?

✅ Linear Regression
❌ K-Means → unsupervised
✅ Decision Trees
✅ Support Vector Machine

5. What is the main goal of unsupervised learning?

✅ Discover hidden structures in data

6. Which of the following are characteristics of K-Means clustering?

❌ Requires labeled data
✅ Minimizes intra-cluster variance
✅ Sensitive to initial centroid placement
❌ Guarantees global optimum

7. In K-Means, if K = 3, how many centroids are initialized?

✅ 3

8. Image 32×32 → Conv layer (3×3 filter, stride 1, no padding)

Output size = 32 − 3 + 1 = 30×30
✅ 30×30

9. Key components of a CNN:

✅ Convolutional layers
✅ Pooling layers
✅ Activation functions
❌ K-Means clustering

10. Which statements about CNNs are correct?

✅ CNNs exploit spatial locality in images
✅ CNNs use shared weights in convolution filters
❌ CNNs require fully connected layers only
✅ CNNs reduce parameters compared to dense networks
